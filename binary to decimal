num = list(input("Enter the number: "))
num = num[::-1]
value = 0
for i in range(len(num)):
    digit = num.pop()
    if digit == '1':
        value = value + pow(2,i)
print("The decimal number of the binary number is :", value)
